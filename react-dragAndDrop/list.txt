import React from "react";
import { Droppable } from "react-beautiful-dnd";

const List = ({ children, title, onDragEnd, name }) => {
  return (
    <div className="flex flex-col">
      <h2 className="text-2xl font-bold mb-2 mx-5">{title}</h2>
      <div className="">
        <Droppable droppableId={name} onDragEnd={onDragEnd}>
          {(provided, snapshot) => (
            <div ref={provided.innerRef} className="h-screen">
              <div className="p-5 rounded-md min-h-max bg-yellow-50 mx-2 gap-y-3 flex flex-col h-screen overflow-auto">
                {children}
                {provided.placeholder}
              </div>
            </div>
          )}
        </Droppable>
      </div>
    </div>
  );
};

export default List;